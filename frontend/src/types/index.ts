export type INavLink = {
    route: string;
    label: string;
};

export type ITableElem = {
  id: string;
  label: string;
};

export type IFilterWindow = {
  id: string;
  label: string;
  icon: string;
}